#include "unregistered_foreign_customer.h"
#include "registered_foreign_customer.h"
#include <cstring>

class unregisteredForeignCustomer::unregisteredForeignCustomer(){
    strcpy(customerName,"");
    strcpy(customerCountry,"");
    strcpy(customerEmailAddress,"");
    customerPhoneNum = 0;
}

unregisteredForeignCustomer::unregisteredForeignCustomer( const char pcustomerName[], const char pcustomerCounty[], int pcustomerPhoneNum[], const char pcustomerEmailAddress[]){
    strcpy(customerName, pcustomerName);
    strcpy(customerCountry, pcustomerCountry);
    strcpy(customerEmailAddress, pcustomerEmailAddress);
    customerPhoneNum = pcustomerPhoneNum;
}

void unregisteredForeignCustomer::registeredUser(){
}

void unregisteredForeignCustomer::searchHotelNPlaces(){
}

unregisteredForeignCustomer::~unregisteredLocalCustomer(){
    //Destructor
}
